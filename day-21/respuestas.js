// Primer ejercicio
function sortNumber(numero) {
  return numero.sort(x,y => x - y);
}

// ultimo ejercicio

/*
function highAndLow(numbers) {
  const high = numbers.split(' ').sort((x, y) =>  x - y);
  return high[high.length-1] + ' ' + high[0];
}

console.log(highAndLow('4 5 29 54 4 0 -214 542 -64 1 -3 6 -6'));
*/

